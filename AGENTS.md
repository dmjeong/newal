# newal 작업 규약

이 파일은 `src/newal/agent/prompts.py`가 시스템 프롬프트에 자동으로 붙입니다.
newal이 스스로 읽고, 이 저장소에서 일하는 다른 에이전트도 읽습니다.

## 한국어

사용자 눈에 닿는 글은 전부 한국어입니다. README, UI 문자열, CLI 메시지가
여기 해당합니다. 코드 주석과 커밋 메시지는 영어로 씁니다.

### 줄표(—)를 문장 중간에 쓰지 않습니다

영어 습관입니다. 한국어는 마침표로 끊거나 쉼표와 괄호를 씁니다.

```
나쁨:  나머지는 다 됩니다 — 두 화면, 설정 저장, 스크립트 생성.
좋음:  나머지는 다 됩니다. 두 화면, 설정 저장, 스크립트 생성 모두 됩니다.

나쁨:  확장이 안 됩니다 — 맞는 지적이었습니다.
좋음:  확장이 안 됩니다. 맞는 지적이었습니다.
```

목록에서 이름과 설명을 가르는 자리는 괜찮습니다.

```
- **3.0** — 파인튜닝 데이터 수집
```

### 번역투를 걷어냅니다

| 쓰지 않습니다 | 대신 |
|---|---|
| ~하는 것입니다 | ~합니다 |
| ~을 통해 | ~(으)로, ~에서 |
| ~에 의해 | 능동으로 바꿔 씁니다 |
| ~되어집니다 | ~됩니다 |
| 가지고 있습니다 | 있습니다 |
| 제공합니다 | 줍니다, 씁니다 |
| 수행합니다 | 합니다 |
| 존재합니다 | 있습니다 |
| ~에 기여합니다 | ~을 올립니다, ~에 도움이 됩니다 |
| ~에 대한 설명 | ~ 설명 |
| ~할 필요가 있습니다 | ~해야 합니다 |

### 한 문장에 한 가지만 담습니다

100자를 넘어가면 대개 두 문장이 붙어 있습니다. 끊습니다.
길게 이어 붙인 수식은 읽는 사람이 되짚어 올라가게 만듭니다.

### 상투구를 반복하지 않습니다

"핵심입니다", "중요합니다", "결국", "사실상"을 문단마다 쓰면 강조가 죽습니다.
한 문서에 한 번이면 충분합니다.

### 주어가 뻔하면 뺍니다

한국어는 주어를 자주 생략합니다. "이 함수는 ~를 반환합니다"보다
"~를 반환합니다"가 자연스럽습니다.

### 숫자와 사실을 앞에 둡니다

```
나쁨:  성능 개선을 위한 최적화가 적용되어 속도가 향상되었습니다.
좋음:  프레임 추출이 3.2초에서 0.8초로 줄었습니다.
```

## 코드

- 주석은 영어로, **왜**를 적습니다. 무엇을 하는지는 코드가 말합니다.
- 사용자에게 보이는 문자열만 한국어입니다. 로그와 예외 메시지는 영어입니다.
- 테스트 이름은 보장하는 내용을 문장으로 적습니다.
  `test_a_tolerated_member_still_refuses_to_serve_a_turn`
- 버그 수정은 재현 테스트를 먼저 넣고, 그 테스트가 옛 코드에서 실패하는지
  확인한 다음 고칩니다.

## 버전

- 기능이나 모델을 크게 바꾸면 브랜치를 새로 파고 주 버전을 올립니다.
- 버그 수정은 패치 자리를 올립니다.
- 커밋 저자는 저장소 소유자입니다. 도구 이름이나 모델 이름을 커밋 메시지,
  PR 본문, 코드 주석에 남기지 않습니다.
