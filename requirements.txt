# Core runtime. Install with:  pip install -r requirements.txt
openai>=1.40
httpx>=0.27
pydantic>=2.7
pyyaml>=6.0
rich>=13.7
typer>=0.12
pillow>=10.3
numpy>=1.26

# Video support (keyframe sampling). Drop this line for text+image only.
opencv-python-headless>=4.9

# Dev / tests
pytest>=8.2
